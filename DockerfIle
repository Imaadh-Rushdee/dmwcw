# Use an official PHP image with Apache
FROM php:8.1-apache

# Set the working directory inside the container
WORKDIR /var/www/html/

# Copy your project files into the container
COPY . /var/www/html/

# Enable Apache mod_rewrite (if using frameworks like Laravel)
RUN a2enmod rewrite

# Install any additional dependencies you need (optional)
# RUN apt-get update && apt-get install -y some-dependency

# Expose the port Apache will run on (usually 80)
EXPOSE 80

# Set the default command to run Apache in the foreground
CMD ["apache2-foreground"]
